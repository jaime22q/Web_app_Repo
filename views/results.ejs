<h1>Results Page</h1>

<ul>
  <% response["data"].items.forEach(item => { %>
    <li><strong>
      <%= item.volumeInfo["title"] %>
      </strong>
    </li>
  <% }) %>
</ul>

<a href="/">Search Again!</a>
